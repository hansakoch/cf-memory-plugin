name: cloudflare-memory
version: 0.1.0
description: "CF Memory Plugin — Cloudflare Agent Memory for AI coding agents and LLM frameworks. Persistent cross-session memory via managed recall, ingest, and summary API."
hooks:
  - on_session_end
